# Auth-Node.js
Node.js server example for user registration and authorization(Back-end).
Database on MonogoDB.
Token Usage.
User registration, user list retrieval, token retrieval are available. 
Middleware functions are implemented to implement access: whether the user is authorized or not, depending on the user's role.
